Везде кроме тестов использвал Автосвязывание через конструктор.
Аннотацию @Autowired всегда писал явно, но я знаю что при единственном контрукторе он автоматически @Autowired.
При запросах на добавление и обноление юзера указывать роли - опционально, но чтобы запрос сработал корректно часть

<gen:roles>
	<gen:name>?</gen:name>
</gen:roles>

надо удалить (Если она создается автоматически как у меня при тестировании через SoapUi). Я бы мог обработать этот момент, чтобы
можно было передавать пустое поле, но я не работал ранее с SOAP и потому, какие практики являются общепринятыми не знаю,
поэтому решил не трогать эту часть.
 
